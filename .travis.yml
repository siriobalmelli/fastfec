language: c
sudo: required
dist: trusty

compiler:
  - clang
  - gcc

matrix:
  include:
    - os: linux
    - os: osx

# only valgrind on Linux for simplicity and speed
addons:
  apt:
    packages:
      - valgrind

script: TRAVIS=1 python3 ./bootstrap.py
